import {atom} from "recoil"

export const laptopListState = atom({
    key : "laptopList",
    default : []
})

export const laptopSpecListState = atom({
    key : "laptopSpecList",
    default : []
})